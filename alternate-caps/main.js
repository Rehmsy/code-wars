//this is wrong, too sleepy to keep going. will revisit.

function capitalize(s){
    var s1 = "";
    var s2 = "";
    for(var i = 0; i < s.length; i++) {
      if (s1 % 2 != 0) {
         s1.toUpperCase();
        }
        else { 
          i++;
        }
      }
    return [s1, s2];
  };